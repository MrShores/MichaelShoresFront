{% extends 'templates/_base.twig' %}

{% block content %}

<div class="container">
    <div class="row">
        <div class="col_8 col_offset_2">
            <div class="article">
                <h1><span>{{ post.title }}</span></h1>
                <div class="content">
                    {{ post.content }}
                </div>
            </div><!-- .article -->
        </div>
    </div>
</div>

{% endblock %}